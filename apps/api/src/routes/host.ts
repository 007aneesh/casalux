import { Hono } from 'hono'

/**
 * host router — see PRD Section(s) for full spec.
 * Controllers live in src/controllers/, services in src/services/
 */
export const hostRouter = new Hono()

// TODO: implement routes per PRD spec
